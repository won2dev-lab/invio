apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: invio-ingress
  namespace: invio
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 8080
