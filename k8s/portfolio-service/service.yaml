apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: invio
spec:
  selector:
    app: portfolio-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
